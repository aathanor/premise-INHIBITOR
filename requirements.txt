# Premise-INHIBITOR dependencies
# Install with: pip install -r requirements.txt

# Web framework + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (async, used for all Ollama calls)
httpx>=0.27.0

# Web UI
gradio>=4.36.0

# Data validation (FastAPI dependency, pinned for stability)
pydantic>=2.7.0
